# These dependencies mirror pyproject.toml [project.dependencies].
# pyproject.toml is the canonical source; this file exists for environments
# that install directly via pip -r without building the package.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pandas>=2.2.2
numpy>=1.26.4
httpx>=0.27.0
