# Release-verification tool constraints. This pins the top-level audit, build,
# lint, type-check, and test tools used by CI and the RC verifier; it is not a
# full transitive lockfile for every runtime dependency.
# Update with:
#   python -m pip index versions <tool>
#   edit this file and pyproject optional-dependencies together
#   python scripts/verify_release_candidate.py
build==1.3.0
mypy==1.19.0
pip-audit==2.10.0
pytest==9.0.3
ruff==0.14.9
types-setuptools==80.9.0.20250822
